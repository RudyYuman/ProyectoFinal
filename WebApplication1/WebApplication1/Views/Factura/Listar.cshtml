@model IEnumerable<Entity_Layer.FacturaBO>

@{
    ViewBag.Title = "Listar";
}

<h2>Listar</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Insertar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_VENTA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FECHA_FACTURA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_VENTA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FECHA_FACTURA)
        </td>
        <td>
            @Html.ActionLink("Editar", "Actualizar", new { ID = item.ID_VENTA }) |
            @Html.ActionLink("Borrar", "Eliminar", new { ID = item.ID_VENTA })
        </td>
    </tr>
}

</table>
