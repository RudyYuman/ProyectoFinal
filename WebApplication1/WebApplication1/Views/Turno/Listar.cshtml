@model IEnumerable<Entity_Layer.TurnoBO>

@{
    ViewBag.Title = "Listar";
}

<h2>Listar</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Insertar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_TURNO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FECHA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_TURNO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FECHA)
        </td>
        <td>
            @Html.ActionLink("Editar", "Actualizar", new { ID = item.Id_TURNO }) |
            
            @Html.ActionLink("Borrar", "Eliminar", new { ID = item.Id_TURNO })
        </td>
    </tr>
}

</table>
