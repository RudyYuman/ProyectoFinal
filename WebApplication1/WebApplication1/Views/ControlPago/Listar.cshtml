@model IEnumerable<Entity_Layer.ControlPagoBO>

@{
    ViewBag.Title = "Listar";
}

<h2>Listar</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Insertar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_CONTROL_PAGO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MONTO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_CONTROL_PAGO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MONTO)
        </td>
        <td>
            @Html.ActionLink("Editar", "Actualizar", new { ID = item.ID_CONTROL_PAGO }) |
            @Html.ActionLink("Borrar", "Eliminar", new { ID = item.ID_CONTROL_PAGO })
        </td>
    </tr>
}

</table>
