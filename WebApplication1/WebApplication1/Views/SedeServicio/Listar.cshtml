@model IEnumerable<Entity_Layer.SedeServicioBO>

@{
    ViewBag.Title = "Listar";
}

<h2>Listar</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Insertar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_SEDE_SERVICIO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NOMBRE_SERVICIO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_SEDE_SERVICIO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NOMBRE_SERVICIO)
        </td>
        <td>
            @Html.ActionLink("Editar", "Actualizar", new { ID = item.ID_SEDE_SERVICIO }) |
            
            @Html.ActionLink("Borrar", "Eliminar", new { ID = item.ID_SEDE_SERVICIO })
        </td>
    </tr>
}

</table>
