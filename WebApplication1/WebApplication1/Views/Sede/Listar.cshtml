@model IEnumerable<Entity_Layer.SedeBO>

@{
    ViewBag.Title = "Listar";
}

<h2>Listar</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Insertar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_SEDE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NOMBRE_SEDE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_SEDE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NOMBRE_SEDE)
        </td>
        <td>
            @Html.ActionLink("Editar", "Actualizar", new { ID = item.ID_SEDE }) |
            
            @Html.ActionLink("Borrar", "Eliminar", new { ID = item.ID_SEDE })
        </td>
    </tr>
}

</table>
